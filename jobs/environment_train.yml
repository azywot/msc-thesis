name: cosmas-train

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - pip
  - git
  - cmake
  - ninja

  - pip:
      # =========================================================
      # CORE RL FRAMEWORK
      # =========================================================
      - verl==0.7.1

      # =========================================================
      # INFERENCE BACKEND (MATCHED TO verl EXPECTATIONS)
      # =========================================================
      - vllm>=0.8.3,<0.9.0

      # =========================================================
      # HF STACK (SAFE ZONE)
      # =========================================================
      - transformers>=4.40,<5.0
      - datasets>=2.20
      - accelerate>=0.30

      # =========================================================
      # CORE UTILITIES
      # =========================================================
      - numpy==1.26.4
      - pandas==2.2.3
      - pyarrow>=15.0.0
      - sympy>=1.13.0

      - wandb>=0.16.0
      - omegaconf>=2.3.0
      - pyyaml>=6.0
      - requests>=2.31.0