name: agent_engine
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      # Core dependencies
      - python-dotenv>=1.0.0
      - vllm>=0.5.0
      - transformers>=4.40.0
      - torch>=2.0.0
      - pyyaml>=6.0
      - numpy>=1.24.0
      - requests>=2.28.0
      - beautifulsoup4>=4.11.0
      - pillow>=9.0.0
      # API providers
      - openai>=1.0.0
      - anthropic>=0.25.0
      # Data and ML
      - datasets>=2.18.0
      - wandb>=0.16.0
      - nano-graphrag==0.0.8.2
      - pandas>=1.5.0
      - pyarrow>=10.0.0
      - math-verify[antlr4_13_2]>=0.7.0
      # Document processing
      - pypdf>=3.0.0
      - python-pptx>=0.6.21
      - openpyxl>=3.0.0
      # Template engine for SLURM jobs
      - jinja2>=3.1.0
      # Development dependencies (optional)
      - pytest>=7.0.0
      - pytest-cov>=4.0.0
      - black>=23.0.0
      - isort>=5.12.0
      - mypy>=1.0.0
