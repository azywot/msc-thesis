# AIME — Qwen3-32B, no tools, orchestrator thinking

name: "NEW_baseline_aime_qwen32B_no_tools_orchestrator"
description: "[Baseline; NO image_inspector, NO mindmap] AIME train with Qwen3-32B, no tools, orchestrator thinking"

slurm:
  partition: "gpu_h100"
  num_gpus: 2
  ntasks: 1
  cpus_per_task: 8
  time: "16:00:00"
  conda_env: "agent_engine"

models:
  orchestrator:
    name: "Qwen3-32B"
    family: "qwen3"
    path_or_id: "Qwen/Qwen3-32B"
    role: "orchestrator"
    tensor_parallel_size: 2  # 64 heads; TP must divide 64.

tools:
  enabled_tools: []
  direct_tool_call: true

dataset:
  name: "aime"
  split: "train"
  data_dir: "./data"
  subset_num: -1

seed: 0
thinking_mode: "ORCHESTRATOR_ONLY"
baseline: true
output_dir: "./experiments/results/NEW_baseline/aime/qwen32B_no_tools_orchestrator"
use_wandb: true
wandb_project: "benchmarks"

cache_dir: "./cache"
