# MATH500 — Qwen3-32B, direct tools, orchestrator thinking

name: "NEW_baseline_math500_qwen32B_direct_tools_orchestrator"
description: "[Baseline; NO image_inspector, NO mindmap] MATH500 test with Qwen3-32B, direct tools, orchestrator thinking"

slurm:
  partition: "gpu_h100"
  num_gpus: 2
  ntasks: 1
  cpus_per_task: 8
  time: "24:00:00"
  conda_env: "agent_engine"

models:
  orchestrator:
    name: "Qwen3-32B"
    family: "qwen3"
    path_or_id: "Qwen/Qwen3-32B"
    role: "orchestrator"
    tensor_parallel_size: 2  # 64 heads; TP must divide 64.

tools:
  enabled_tools:
    - web_search
    - code_generator
  direct_tool_call: true

dataset:
  name: "math500"
  split: "test"
  data_dir: "./data"
  subset_num: -1

seed: 0
thinking_mode: "ORCHESTRATOR_ONLY"
baseline: true
output_dir: "./experiments/results/NEW_baseline/math500/qwen32B_direct_tools_orchestrator"
use_wandb: true
wandb_project: "benchmarks"

cache_dir: "./cache"
