# AIME Sub-Agent Mode — Qwen3-8B for all text roles (no images)

name: "aime_qwen3_subagent"
description: "AIME with sub-agent mode using Qwen3-8B for orchestrator and tool agents"

slurm:
  partition: "gpu_h100"
  num_gpus: 1
  ntasks: 1
  cpus_per_task: 8
  time: "08:00:00"
  conda_env: "agent_engine"

models:
  orchestrator:
    name: "Qwen3-8B"
    family: "qwen3"
    path_or_id: "Qwen/Qwen3-8B"
    role: "orchestrator"

tools:
  enabled_tools:
    - web_search
    - code_generator
    - mind_map
  direct_tool_call: false

dataset:
  name: "aime"
  split: "train"
  data_dir: "./data"
  subset_num: -1  # -1 for all

seed: 0
thinking_mode: "ORCHESTRATOR_ONLY"

output_dir: "./experiments/results/aime_qwen3_subagent"
use_wandb: true
wandb_project: "msc-thesis"

cache_dir: "./cache"
