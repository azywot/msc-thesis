# HLE Sub-Agent Mode — Qwen3-4B for all text roles, Qwen2.5-VL-3B for images

name: "hle_qwen3_subagent"
description: "HLE with sub-agent mode using Qwen3-4B for orchestrator and tool agents (text + vision)"

slurm:
  partition: "gpu_h100"
  num_gpus: 2
  ntasks: 1
  cpus_per_task: 8
  time: "14:00:00"
  conda_env: "agent_engine"

models:
  orchestrator:
    name: "Qwen3-4B"
    family: "qwen3"
    path_or_id: "Qwen/Qwen3-4B"
    role: "orchestrator"

  # web_search, code_generator, text_inspector, mind_map are omitted —
  # they share Qwen3-4B with the orchestrator and will reuse its cached instance.

  image_inspector:
    name: "Qwen/Qwen3-VL-4B-Instruct"
    family: "qwen3"                          # Uses Qwen chat-template logic
    path_or_id: "Qwen/Qwen3-VL-4B-Instruct"
    role: "image_inspector"

tools:
  enabled_tools:
    - web_search
    - code_generator
    - text_inspector
    - image_inspector
    - mind_map
  direct_tool_call: false

dataset:
  name: "hle"
  split: "test_subset_200"
  data_dir: "./data"
  subset_num: -1  # -1 for all

seed: 0
thinking_mode: "ORCHESTRATOR_ONLY"

output_dir: "./experiments/results/hle_qwen3_subagent"
use_wandb: true
wandb_project: "msc-thesis-benchmarks"

cache_dir: "./cache"
