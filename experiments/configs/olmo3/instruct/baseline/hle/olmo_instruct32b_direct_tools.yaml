# HLE — OLMo-3.1-32B-Instruct, direct tools

name: "OLMo_Instruct_baseline_hle_olmo_instruct32b_direct_tools"
description: "[OLMo-Instruct Baseline; NO image_inspector, NO mindmap] HLE test_subset_200 with OLMo-3.1-32B-Instruct, direct tools"

slurm:
  partition: "gpu_h100"
  num_gpus: 2
  ntasks: 1
  cpus_per_task: 8
  time: "24:00:00"
  conda_env: "agent_engine"

models:
  orchestrator:
    name: "OLMo-3.1-32B-Instruct"
    family: "olmo-instruct"
    path_or_id: "allenai/Olmo-3.1-32B-Instruct"
    role: "orchestrator"
    tensor_parallel_size: 2  # 64 heads; TP must divide 64.

tools:
  enabled_tools:
    - web_search
    - code_generator
    - text_inspector
  direct_tool_call: true

dataset:
  name: "hle"
  split: "test_subset_200"
  data_dir: "./data"
  subset_num: -1

seed: 0
baseline: true
output_dir: "./experiments/results/olmo/instruct/baseline/hle/olmo_instruct32b_direct_tools"
use_wandb: true
wandb_project: "benchmarks"

cache_dir: "./cache"
