# AIME — DeepSeek-R1-Distill-Qwen-32B, no tools, thinking disabled

name: "DS_baseline_aime_ds32b_no_tools_none"
description: "[DeepSeek-R1 Baseline; NO image_inspector, NO mindmap] AIME train with DeepSeek-R1-Distill-Qwen-32B, no tools, thinking disabled"

slurm:
  partition: "gpu_h100"
  num_gpus: 2
  ntasks: 1
  cpus_per_task: 8
  time: "24:00:00"
  conda_env: "agent_engine"

models:
  orchestrator:
    name: "DeepSeek-R1-Distill-Qwen-32B"
    family: "deepseek"
    path_or_id: "deepseek-ai/DeepSeek-R1-Distill-Qwen-32B"
    role: "orchestrator"
    tensor_parallel_size: 2  # TP must divide num attention heads.

tools:
  enabled_tools: []
  direct_tool_call: true

dataset:
  name: "aime"
  split: "train"
  data_dir: "./data"
  subset_num: -1

seed: 0
thinking_mode: "NO"
baseline: true
output_dir: "./experiments/results/deepseek/baseline/aime/ds32b_no_tools_none"
use_wandb: true
wandb_project: "benchmarks"

cache_dir: "./cache"
