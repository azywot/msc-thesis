# HLE — DeepSeek-R1-Distill-Qwen-32B, sub-agent tools, upstream-aligned.
#
# Mirrors the ``multi-agent-tools`` (MAT) setup as closely as possible while
# keeping the thesis's JSON_SINGLE tool-call format (DeepSeek is hard-wired to
# it). Concretely:
#
#   * ``baseline: true`` + ``direct_tool_call: false`` gives a vanilla growing
#     chat (no AgentFlow planning turn) where search results are distilled by
#     the web_search sub-agent LLM — the thesis analog of upstream's
#     ``generate_webpage_to_reasonchain_batch`` summariser.
#   * Tool suite matches ``get_hard_question_instruction`` exactly:
#     ``web_search + code_generator + mind_map`` (text_inspector dropped).
#   * Sampling matches upstream (config.py + run_agentic_reason.py runtime
#     override for ``max_tokens``): T=0.7, top_p=0.8, top_k=20,
#     repetition_penalty=1.1, max_tokens=8192.
#   * ``chat_template_override`` reproduces upstream's role-stripping Jinja
#     template from ``scripts/agentic_reason/prompt_manager.py``. When set,
#     the DeepSeek ``<think>`` prefix injection is skipped (strict match).
#   * ``prompt_template_override: hle_mat`` selects a new system-prompt YAML
#     whose wording echoes ``get_hard_question_instruction`` +
#     ``get_task_instruction_openqa``, with the biotech-revenue worked
#     example translated to JSON_SINGLE sub-agent tool calls.
#
# Intended comparison twin against ``ds32b_direct_tools_*`` (thesis default) and
# as the "apples-to-apples" run versus ``multi-agent-tools`` on HLE.

name: "DS_baseline_hle_ds32b_subagent_tools_aligned"
description: "[DeepSeek-R1 Baseline, multi-agent-tools-aligned] HLE test_subset_200 with DS-R1-Distill-Qwen-32B, sub-agent tools (web_search + code_generator + mind_map), upstream sampling + chat template + prompt wording."

slurm:
  partition: "gpu_h100"
  num_gpus: 2
  ntasks: 1
  cpus_per_task: 8
  time: "24:00:00"
  conda_env: "agent_engine"

models:
  orchestrator:
    name: "DeepSeek-R1-Distill-Qwen-32B"
    family: "deepseek"
    path_or_id: "deepseek-ai/DeepSeek-R1-Distill-Qwen-32B"
    role: "orchestrator"
    tensor_parallel_size: 2
    # Upstream sampling (multi-agent-tools/scripts/agentic_reason/config.py +
    # run_agentic_reason.py runtime override to max_tokens=8192). These
    # explicitly overwrite the ``deepseek`` family defaults
    # (T=0.6, top_p=0.95, max_tokens=32768).
    temperature: 0.7
    top_p: 0.8
    top_k: 20
    repetition_penalty: 1.1
    max_tokens: 8192
    # Exact Jinja chat template used by ``prepare_input_prompts`` in
    # ``multi-agent-tools/scripts/agentic_reason/prompt_manager.py``. Strips
    # every role marker; content is concatenated with a single trailing space
    # between messages and an EOS at the end. Activating this also suppresses
    # the thesis's ``<think>\n`` / ``<think>\n\n</think>\n`` post-render
    # prefix injection for DeepSeek (see VLLMProvider._render_messages).
    chat_template_override: "{% for message in messages %}{% if message['role'] == 'user' %}{{ ' ' }}{% endif %}{{ message['content'] }}{% if not loop.last %}{{ '  ' }}{% endif %}{% endfor %}{{ eos_token }}"

tools:
  enabled_tools:
    - web_search
    - code_generator
    - mind_map
  direct_tool_call: false
  web_tool_provider: "serper"
  max_search_limit: 10
  top_k_results: 5
  max_doc_len: 3000

dataset:
  name: "hle"
  split: "test_subset_200"
  data_dir: "./data"
  subset_num: -1

max_turns: 15
seed: 0
# thinking_mode is effectively a no-op under chat_template_override (the
# <think> prefix injection path is bypassed); left as NO to match upstream,
# which never force-opens a thinking block.
thinking_mode: "NO"
baseline: true

# Pick the upstream-aligned system-prompt template instead of
# ``gaia_baseline.yaml`` (the default routing for dataset.name == "hle").
prompt_template_override: "hle_mat"

output_dir: "./experiments/results/deepseek/baseline/hle/ds32b_subagent_tools_aligned"
use_wandb: true
wandb_project: "benchmarks"

cache_dir: "./cache"
