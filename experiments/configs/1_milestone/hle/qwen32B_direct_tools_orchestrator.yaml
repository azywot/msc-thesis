# HLE — Qwen3-32B, direct tool call, orchestrator thinking

name: "hle_qwen32B_direct_tools_orchestrator"
description: "HLE subset_200 with Qwen3-32B, direct tools, orchestrator thinking"

slurm:
  partition: "gpu_h100"
  num_gpus: 2
  ntasks: 1
  cpus_per_task: 8
  time: "16:00:00"
  conda_env: "agent_engine"

models:
  orchestrator:
    name: "Qwen3-32B"
    family: "qwen3"
    path_or_id: "Qwen/Qwen3-32B"
    role: "orchestrator"
    tensor_parallel_size: 2  # 64 heads; TP must divide 64.

tools:
  enabled_tools:
    - web_search
    - code_generator
    - text_inspector
    - mind_map
  direct_tool_call: true

dataset:
  name: "hle"
  split: "test_subset_200"
  data_dir: "./data"
  subset_num: -1

seed: 0
thinking_mode: "ORCHESTRATOR_ONLY"

output_dir: "./experiments/results/1_milestone/hle/qwen32B_direct_tools_orchestrator"
use_wandb: true
wandb_project: "benchmarks"

cache_dir: "./cache"
